# CLAUDE.md — Smart Parking Prizren

Ky skedar lexohet automatikisht nga Claude Code në çdo sesion. Mbaje të përditësuar pas çdo vendimi të madh arkitekturor.

## 1. Konteksti i projektit
Platformë real-time për parkim inteligjent në Prizren — projekti #2 i portofolit (Frontend/Full-Stack Developer), pas "Prizren Smart City". Tregon në kohë reale vendparkimet e lira/zëna në hartë, lejon rezervim, dhe i jep administratorëve panel analitik.

**Diferencuesi kryesor**: arkitekturë real-time e vërtetë (WebSocket + Redis pub/sub), jo polling.

## 2. Tech Stack (i vendosur)

**Frontend** (`smart-parking-frontend/`):
- Next.js (App Router), TypeScript, TailwindCSS + shadcn/ui
- React Query (server state) + Zustand (client/UI state)
- socket.io-client, MapLibre GL (+ react-map-gl) për hartën

**Backend** (`smart-parking-backend/`):
- NestJS + TypeScript (ESM, `type: module`)
- WebSocket Gateway (Socket.io adapter i integruar)
- PostgreSQL + PostGIS
- Prisma ORM — **i fiksuar në `^6.19.3`**. Arsye: `prisma`/`@prisma/client` v7+ e zhvendos CLI-n drejt "Prisma Developer Platform" (cloud-first: `deploy`, `contract`, `postgres` si shërbim menaxhuar), duke hequr workflow-in klasik lokal (`prisma init` s'krijon më `schema.prisma`, `migrate`/`generate` fshihen pas `orm`). Ne përdorim Postgres+PostGIS self-hosted në Docker, jo Prisma Postgres cloud, prandaj v6 (linja klasike ORM) është zgjedhja e saktë deri sa arkitektura jonë të kërkojë platformën cloud.
- Redis — pub/sub + caching (`ioredis`, `@socket.io/redis-adapter`)
- JWT auth (access + refresh) + RBAC (citizen / attendant / admin) — `@nestjs/jwt`, `@nestjs/passport`, `passport-jwt`
- class-validator / class-transformer për validim inputesh
- BullMQ (opsionale, fazë e mëvonshme) për job queue

**Infra & DevOps:**
- Docker Compose lokal (postgres+postgis, redis, backend, frontend, target `dev` — shih §8 Faza 12) — shih `docker-compose.yml`
- Deploy: frontend → Vercel; backend+redis+postgres → Railway/Render. Dockerfile-t prodhimi ✅ Faza 12 (shih §8); ekzekutimi real i deploy-it (llogari Vercel/Railway) mbetet detyrë e Gent-it — udhëzues hap-pas-hapi te README.md §Deployment.

## 3. Modeli i të dhënave (draft — shtohet fazë pas faze te `smart-parking-backend/prisma/schema.prisma`)
- **User** (id, email, passwordHash, role: citizen | attendant | admin, hashedRefreshToken, createdAt, updatedAt) — ✅ Faza 1
- **ParkingZone** (id, emri, `polygon` PostGIS `geometry(Polygon,4326)`, createdAt, updatedAt) — ✅ Faza 2
- **ParkingSpot** (id, kodi, `location` PostGIS `geometry(Point,4326)`, statusi: free|occupied|reserved|disabled, zoneId, unik (zoneId,code)) — ✅ Faza 2
- **ParkingSession** (id, spotId, userId?, checkIn, checkOut?, burimi: sensor|manual|qr) — ✅ Faza 4 (userId opsional në skemë, por check-in manual gjithmonë e vendos)
- **Reservation** (id, spotId, userId, startTime, endTime, statusi: confirmed|cancelled) — ✅ Faza 5
- **SensorEvent** (id, spotId, statusi, timestamp) — ✅ Faza 3
- **Notification** (id, userId, tipi, mesazhi, lexuar, createdAt) — ✅ Faza 7
- **AuditLog** (id, action, actorId?, metadata?, timestamp) — ✅ Faza 8

## 4. Arkitektura real-time — ✅ Faza 3
- "Sensor simulator" (`modules/sensor-simulator/`, `@nestjs/schedule` `@Interval`) ndryshon rastësisht statusin e 1-2 vendparkimeve çdo `SENSOR_SIMULATOR_INTERVAL_MS` (default 8s, `SENSOR_SIMULATOR_ENABLED` për ta çaktivizuar) — simulon sensorë IoT, vetëm free↔occupied (reserved/disabled janë gjendje biznesi, jo sensori).
- Check-in/check-out manual nga qytetarët (buton "Parkova këtu"/"Dola", `modules/sessions/`) — ✅ Faza 4, burim i dytë i vërtetë i të dhënave. Ripërdor saktësisht të njëjtin pipeline Redis→Gateway→WS të sensorit (shih më poshtë) — check-in/check-out publikojnë te i njëjti `SPOT_STATUS_CHANNEL`, kështu që frontend-i s'ka fare logjikë të veçantë "live" për burim manual vs. sensor. QR mbetet vetëm si vlerë e mundshme e `source` në API (`CheckInDto.source: 'manual'|'qr'`) — skanimi/UI i QR-it vetë s'u ndërtua (jashtë fushës së Fazës 4, "buton" e mbulon kërkesën "QR ose buton").
- Çdo ndryshim statusi → shkruhet në DB + `SensorEvent` → publikohet në Redis pub/sub channel (`parking:spot-status`, `modules/redis/`) → NestJS Gateway (`modules/realtime/realtime.gateway.ts`) e merr si subscriber → WebSocket `spot:update` te klientët në "room"-in e zonës (Socket.io room = zoneId).
- Frontend: `useParkingSocket(zoneIds)` bashkohet me room-at, dhe në `spot:update` **patch-on direkt cache-in e React Query** (`setQueryData`, pa refetch) — real-time i vërtetë. `ParkingMap` bën "flash" të vogël (rreze+stroke më i madh, 1.2s) me `maplibre.setFeatureState` kur statusi i një spoti ndryshon.
- Redis pub/sub (jo broadcast in-memory) përgatit shkallëzimin horizontal: çdo instancë backend pajtohet te i njëjti channel dhe transmeton vetëm te socket-et e veta lokale në room-in përkatës — nuk kërkohet `@socket.io/redis-adapter` shtesë për korrektësi cross-instance me këtë design (paketa mbetet e instaluar për nevoja të mundshme më vonë).
- **Njoftimet (Faza 7)** përdorin **të njëjtin pipeline**: `NotificationsService.create()` shkruan DB + publikon te `parking:notification` (Redis) → `RealtimeGateway` e merr si subscriber → emeton `notification:new` te room-i privat `user:{userId}`. Ky room mbushet vetëm nëse socket-i lidhet me JWT të vlefshëm në `handshake.auth.token` (`RealtimeGateway.handleConnection` e verifikon me `JwtService`); lidhje pa token/token i pavlefshëm mbetet anonime (harta publike vazhdon të punojë). Krijuar automatikisht nga check-in/check-out (`SessionsService`) dhe krijim/anulim rezervimi (`ReservationsService`), plus një job kujtese (`NotificationsService.checkReminders`, `@Interval` çdo 60s) që njofton 15 min para fillimit të një rezervimi (`Reservation.reminderSent` mbron nga dërgimi i përsëritur).

## 5. Struktura e folderave (aktuale)

```
smart-parking-prizren/
├── smart-parking-frontend/
│   ├── app/
│   │   ├── (public)/page.tsx ✅ (harta statike), (public)/zones/[id]/ ✅ (detaje+listë spotesh+rezervim), (public)/reservations/ ✅ (rezervimet e mia), (public)/notifications/ ✅
│   │   ├── (auth)/login/ ✅, (auth)/register/ ✅, (auth)/forgot-password/ ✅, (auth)/reset-password/ ✅, (auth)/verify-email/ ✅
│   │   ├── (dashboard)/admin/ ✅ layout.tsx (sidebar operacionesh, rol-gated, mobile sheet) + page.tsx (overview+KPI) + zones/, spots/, analytics/, audit-log/ (secilja rirendit panel-in përkatës), (dashboard)/attendant/
│   │   └── layout.tsx
│   ├── components/{map/ParkingMap.tsx ✅ (me flash live, basemap OSM),map/HeatmapMap.tsx ✅ (layer "heatmap" nativ MapLibre, Faza 9, basemap OSM),nav/Header.tsx ✅ (zile njoftimesh live, meny mobile Faza 10, LiveIndicator),dashboard/AdminSidebar.tsx ✅ (nav e grupuar + collapse, iniciativa redesign 2026-08-30),realtime/LiveIndicator.tsx ✅ (gjendje reale e socket-it, jo e simuluar),ui/{button.tsx,skeleton.tsx ✅ (Faza 10)},providers}/
│   ├── hooks/{useZones.ts ✅ (+mutime admin, invalidon auditLogs),useSpots.ts ✅ (+mutime admin, invalidon auditLogs),useParkingSocket.ts ✅ (patch cache live),useConnectionStatus.ts ✅ (connected/connecting/disconnected nga eventet e socket.io),useSessions.ts ✅,useReservations.ts ✅,useAdminStats.ts ✅,useNotifications.ts ✅ (+socket privat live),useAuditLogs.ts ✅,useAnalytics.ts ✅ (heatmap+peak-hours)}
│   ├── lib/{api.ts ✅ (auth header+refresh-retry),auth.ts ✅,sessions.ts ✅,reservations.ts ✅,admin.ts ✅,notifications.ts ✅,zones.ts ✅ (+CRUD shkrimi),spots.ts ✅ (+CRUD shkrimi),audit-log.ts ✅,analytics.ts ✅,map-style.ts ✅ (Faza 10, basemap OSM raster i përbashkët),types.ts,socket.ts ✅ (auth JWT në handshake)}
│   ├── store/{useUiStore.ts ✅ (+adminSidebarCollapsed, persist), useAuthStore.ts ✅ (zustand persist → localStorage)}
│   └── Dockerfile ✅ (multi-stage: target `dev` + prodhim me output standalone, Faza 12), .dockerignore ✅, .env.local, .env.example
│
├── smart-parking-backend/
│   ├── src/
│   │   ├── modules/
│   │   │   ├── auth/ ✅ (controller, service, dto, strategies, types; +forgot-password/reset-password/verify-email/resend-verification, iniciativa redesign)
│   │   │   ├── email/ ✅ (EmailService mbi Resend SDK, no-op nëse s'ka RESEND_API_KEY, iniciativa redesign)
│   │   │   ├── users/ ✅ (service, mapper)
│   │   │   ├── zones/ ✅ (CRUD raw-SQL + PostGIS, admin-only shkrim)
│   │   │   ├── spots/ ✅ (CRUD raw-SQL + PostGIS, admin-only shkrim; exports SpotsService)
│   │   │   ├── realtime/realtime.gateway.ts ✅ (Socket.io Gateway: zone:join/leave, spot:update, auth JWT në handshake → room privat user:{id}, notification:new)
│   │   │   ├── sensor-simulator/ ✅ (`@Interval` worker, free↔occupied; përjashton spote me sesion aktiv)
│   │   │   ├── sessions/ ✅ (check-in/check-out, transaksion + compare-and-swap mbi status; refuzon check-in nëse spoti ka rezervim aktiv i dikujt tjetër)
│   │   │   ├── reservations/ ✅ (validim konfliktesh kohore, cancel, /spot/:id/upcoming publik)
│   │   │   ├── admin/ ✅ (GET /admin/stats — admin-only, groupBy Prisma pa SQL raw; GET /admin/analytics/heatmap+peak-hours — Faza 9, SQL raw)
│   │   │   ├── notifications/ ✅ (REST + publikim Redis + job kujtese @Interval)
│   │   │   └── audit-log/ ✅ (GET /audit-logs admin-only; AuditLogService.record() thirret nga auth/zones/spots)
│   │   ├── redis/{redis.module.ts,redis.service.ts,redis-channels.ts} ✅ (@Global, publisher+subscriber ioredis)
│   │   ├── common/{guards,decorators}/ ✅ (JwtAuthGuard, JwtRefreshGuard, RolesGuard, RateLimitGuard (global, APP_GUARD) + @Roles(), @CurrentUser(), @RateLimit())
│   │   ├── common/{dto,validators}/ ✅ (GeoPointDto, GeoPolygonDto + validatorë GeoJSON)
│   │   ├── common/passport-global.module.ts ✅ (PassportModule global, shih §8)
│   │   ├── common/{filters,pipes}/ (bosh)
│   │   ├── config/
│   │   ├── prisma/{prisma.module.ts,prisma.service.ts}
│   │   ├── app.module.ts (+ScheduleModule.forRoot()), main.ts (ValidationPipe global + CORS + Swagger `/docs` ✅ Faza 13)
│   │   └── prisma/{schema.prisma, seed.js} (User+Role, ParkingZone, ParkingSpot+SpotStatus, SensorEvent; `npm run db:seed`)
│   ├── test/ ✅ (Faza 11, `*.e2e-spec.ts`: auth, sessions, reservations + `utils/` helpers — shih §8; `*.spec.ts` unit janë pranë çdo moduli te `src/`)
│   └── Dockerfile ✅ (multi-stage: target `dev` + prodhim me `migrate deploy` në CMD, Faza 12), .dockerignore ✅
│
├── docker-compose.yml
├── CLAUDE.md
├── .claude/launch.json  (preview_start: "frontend", npm --prefix smart-parking-frontend run dev)
└── README.md
```

## 6. Roadmap me faza (kërko aprovim para se të kalosh në fazën tjetër)
- [x] **Faza 0:** Konfirmim arkitekture + scaffold i dy projekteve + Docker Compose + CLAUDE.md
- [x] **Faza 1:** Auth (JWT + refresh + RBAC) + modeli i User
- [x] **Faza 2:** Zones & Spots CRUD + PostGIS + harta bazë (statike)
- [x] **Faza 3:** WebSocket Gateway + Redis pub/sub + sensor simulator → harta bëhet live
- [x] **Faza 4:** Check-in/check-out manual (QR/buton)
- [x] **Faza 5:** Rezervimet (booking) + validim konfliktesh kohore
- [x] **Faza 6:** Admin dashboard
- [x] **Faza 7:** Njoftimet
- [x] **Faza 8:** Audit logs + rate-limiting/anti-abuse
- [x] **Faza 9:** Analitika e avancuar (heatmap PostGIS)
- [x] **Faza 10:** Polish UI/UX
- [x] **Faza 11:** Testim (unit + e2e) — vetëm backend, shih §8
- [x] **Faza 12:** Dockerizim final + udhëzues deploy — shih §8
- [x] **Faza 13:** Dokumentim (Swagger/OpenAPI + diagram Mermaid) — shih §8

**Roadmap-i i planifikuar fillimisht (§6 origjinale, Faza 0) është plotësisht i mbyllur.** Çdo punë e mëtejshme (hardening shtesë, feature të reja, vizatim zonash mbi hartë i shtyrë nga Faza 10, etj.) trajtohet si kërkesë e re individuale nga Gent, jo si vazhdim automatik i numërtimit të fazave.

## 7. Rregulla pune
- Para se të shkruash kod për një fazë, propozo shkurt planin dhe prit aprovimin.
- Mos e "hollosh" arkitekturën real-time drejt polling-ut për lehtësi — nëse ka pengesë teknike, thuaje hapur dhe propozo alternativë.
- TypeScript strikt (pa `any` pa arsye), Zod/class-validator për çdo input.
- Kod production-grade — trajto rastet kufitare (spot i zënë, WebSocket i shkëputur, konflikt rezervimi, etj.)
- Mbaje `CLAUDE.md` të përditësuar pas çdo vendimi të madh arkitekturor.
- Gent bën `git add/commit/push` vetë — vetëm sugjero mesazhet e commit-it dhe komanda git read-only.
- `.env.example` gjithmonë; kurrë sekrete reale në kod.

## 8. Vendime arkitekturore (log)
- **2026-08-29** — Prisma i fiksuar në `^6.x` (jo latest/v7+). Shih arsyetimin te seksioni 2.
- **2026-08-29** — Backend i skaffoldur me Nest CLI më të fundit → vjen me `vitest` (jo jest) dhe `oxlint` (jo eslint) si default; mbajtur si janë, s'ka arsye teknike për t'i ndryshuar.
- **2026-08-29** — Harta: zgjedhur **MapLibre GL + react-map-gl** (jo Leaflet) — vektor-based, performancë më e mirë për shumë markera live-update.
- **2026-08-29** — Porti i host-it për Postgres në `docker-compose.yml` ndryshuar në `5433` (nga default 5432), konfigurueshëm via `POSTGRES_PORT`. Arsye: konteneri `prizren-postgres` i projektit "Prizren Smart City" e zë 5432 në këtë makinë; brenda rrjetit të docker-it backend-i vazhdon të lidhet te `postgres:5432` (pa ndryshim).
- **2026-08-29** — Faza 1 (Auth): `User` model + enum `Role` në Prisma; migrimi `20260829013937_add_user_model` u aplikua (me konfirmim eksplicit të Gent-it, pasi Prisma vetë e bllokoi `migrate reset` si veprim të rrezikshëm nga një agjent AI). `AuthModule` përdor `PassportModule.register({ defaultStrategy: 'jwt' })` (jo `PassportModule` bosh) — pa `.register()`, `AuthModuleOptions` s'ofrohet fare si provider dhe `JwtRefreshGuard`/`JwtAuthGuard` dështojnë në DI edhe pse parametri është `@Optional()`. Refresh token ruhet i hash-uar (bcrypt) te `User.hashedRefreshToken`, rrotullohet në çdo `/auth/refresh`, dhe pastrohet në `/auth/logout` (revokim i menjëhershëm). Testuar end-to-end me curl: register/login/me/refresh/logout/revocation — të gjitha OK.
- **2026-08-29** — Faza 2 (Zones & Spots): `polygon`/`location` në Prisma janë `Unsupported("geometry(...)")` (Prisma s'i hidraton dot tipet PostGIS) → CRUD-i bëhet me `$queryRaw`/`$executeRaw` (`ST_GeomFromGeoJSON`/`ST_AsGeoJSON`), parametrizuar (pa injektim SQL). Shtuar indekse GiST (`parking_zones_polygon_gist_idx`, `parking_spots_location_gist_idx`) për kërkime gjeografike të shpejta (migrim i veçantë `add_spatial_indexes`, jo i gjeneruar nga Prisma). `JwtAuthGuard`/`RolesGuard` përdorur via `@UseGuards()` në module TË TJERA nga AuthModule (zones, spots) shkaktonin të njëjtin gabim DI si më sipër, sepse Nest i instancion guard-et me injector-in e modulit pritës — jo AuthModule. Zgjidhur duke krijuar `PassportGlobalModule` (`@Global()`, mbështjell `PassportModule.register({defaultStrategy:'jwt'})`) të importuar një herë në `AppModule`; kjo e bën `AuthModuleOptions` të disponueshëm kudo. Validim GeoJSON me validatorë të personalizuar class-validator (`common/validators/geojson.validator.ts`) — jo libreri e jashtme, mjaftonte për Point/Polygon. Krijuar `prisma/seed.js` (JS i thjeshtë, jo TS — CLI-ja e re e Prisma-s s'ka `ts-node` të integruar dhe seed-i s'ka nevojë për dekoratorë Nest) me 2 zona + 9 spote demo në Prizren + user admin (`admin@smartparking.rks` / `AdminPrizren2026!`, vetëm dev). **Harta**: `demotiles.maplibre.org` (stili fillestar) nuk arrinte kurrë event-in `load` në browser-in e sandbox-uar të testimit (edhe me stil krejt bosh pa burime, WebGL context krijohej por `readPixels` tregonte canvas plotësisht bosh — kufizim i mjedisit të testimit, jo bug në kod, i verifikuar duke inspektuar drejtpërdrejt gjendjen e brendshme të maplibre-gl: `style._loaded=true`, source-t me GeoJSON korrekt, layers në rend të saktë). Zgjidhje: stil bazë inline bosh (`BLANK_STYLE`, pa asnjë kërkesë rrjeti) + `fitBounds` mbi bbox-in real të zonave/spoteve në vend të një qendre/zoom fiks. Rekomandohet të verifikohet vizualisht në browser real të Gent-it; nëse dëshirohet basemap i vërtetë (OSM/MapTiler), shtohet si "polish" (Faza 10). Testuar CRUD+RBAC+validim+cascade-delete plotësisht me curl (200/201/400/401/403/404/409 siç pritej).
- **Shënim mjedisi**: `npm run start:dev` (Nest `--watch`) nën Windows shpesh e le procesin `node` të varur pas `TaskStop`/Ctrl-C — porti (3001) mbetet i zënë. Kontrollo `Get-NetTCPConnection -LocalPort 3001` dhe bëj `Stop-Process` para se të rinisësh serverin.
- **2026-08-29** — Faza 3 (Real-time): `RedisService` mban 2 lidhje `ioredis` (`publisher`/`subscriber`) — një klient në modalitet subscribe s'mund të lëshojë komanda të tjera, prandaj ndahen. `RealtimeGateway` pajtohet te channel-i `parking:spot-status` në `onModuleInit` dhe transmeton `spot:update` te `server.to(zoneId)`; **s'e prek DB-në** — shkrimi bëhet nga `SensorSimulatorService`, gateway-i vetëm ripërcjell. `SpotsModule` s'e eksportonte `SpotsService` (s'kishte nevojë deri në Fazën 2) — u shtua `exports: [SpotsService]` që `SensorSimulatorModule` ta injektojë. `@Interval()` e Nest-it merr vlerën e intervalit në kohën e ngarkimit të klasës (jo via DI/ConfigService) → përdoret `process.env.SENSOR_SIMULATOR_INTERVAL_MS` direkt (mjafton, pasi `ConfigModule.forRoot()` e populon `process.env` që në nisje). Migrimi i `SensorEvent` fillimisht donte të fshinte indekset GiST (Prisma s'i njeh si "të qëllimshme" pasi janë mbi kolona `Unsupported`) — u redaktua migration.sql para aplikimit për t'i ruajtur. Frontend: `import { Map } from "react-map-gl/maplibre"` përplasej me `Map` globale të JS-it (përdorur për `prevStatusRef`/`flashTimeoutsRef`) → riemërtuar `MapGL`. `react-hooks` (eslint) e re s'lejon mutim të `ref.current` gjatë render-it (`onSpotUpdateRef.current = onSpotUpdate`) → zhvendosur brenda `useEffect`. **Verifikuar tërësisht**: script Node me `socket.io-client` konfirmoi zinxhirin e plotë (simulator→DB→SensorEvent→Redis→Gateway→WS→klient); browser (DOM/tekst, jo WebGL) konfirmoi që statuset në listën e spoteve ndryshojnë live pa refresh faqeje.
- **2026-08-29** — Faza 4 (Check-in/check-out): titulli i fazës përmend eksplicit "buton", çka nënkupton UI — dhe check-in kërkon domosdoshmërisht qytetar të kyçur, por frontend-i deri atëherë s'kishte fare autentikim të lidhur (Faza 1 ishte vetëm backend). Prandaj Faza 4 përfshiu edhe telajimin minimal të auth-it në frontend: `useAuthStore` (zustand + `persist` → localStorage), `lib/api.ts` bashkangjit `Authorization` automatikisht dhe provon një rifreskim token-i (`/auth/refresh`) + ritentativë të vetme te 401. **Thjeshtim i qëllimshëm**: token-at (access+refresh) ruhen në localStorage, jo httpOnly cookie — më i lehtë pasi backend-i tashmë i kthen në trupin JSON (Faza 1); hardening me httpOnly cookie do të ishte ndryshim arkitekturor më i madh, i lënë për diskutim të mëvonshëm nëse duhet (jo Faza 4). `ParkingSession`: check-in/check-out bëhen brenda `prisma.$transaction()`, me "compare-and-swap" (`UPDATE ... WHERE status='free'`, kontrollon rreshtat e prekur) për mbrojtje nga race condition kur dy qytetarë bëjnë check-in njëkohësisht në të njëjtin spot — nëse `affected===0`, hidhet `ConflictException` dhe transaksioni bën rollback (edhe krijimi i sesionit anulohet). Sensor simulator (Faza 3) u përditësua të përjashtojë spote me sesion aktiv (`findRandomTogglable`) — përndryshe do t'i "lironte" vetë spotet ku dikush është realisht i parkuar. **Bug real i gjetur e rregulluar në browser**: kur `/sessions/me/active` kthen `null`, Express/Nest dërgon trup krejt bosh (`Content-Length: 0`), jo stringun `"null"`; `apiFetch`-i e kthente këtë si `undefined`, dhe React Query e injoron `undefined` si "pa përditësim" — UI-ja mbante "Dola" edhe pas check-out-it të suksesshëm derisa të rifreskohej faqja. Rregulluar duke kthyer `null` (jo `undefined`) për trup bosh te `parseBody`. Testuar plotësisht: curl (201/409×2/200/404/401) + browser i vërtetë (regjistrim→login→check-in→verifikim UI (buton zhduket kudo tjetër)→check-out→verifikim (buton kthehet "Parkova këtu", pa "Dola" të mbetur)→logout).
- **2026-08-29** — Faza 5 (Rezervimet): `Reservation` s'e ndryshon `ParkingSpot.status` në kohën e krijimit — statusi pasqyron gjendjen FIZIKE aktuale (sensor/check-in), jo prenotime të ardhshme; do të ishte gabim ta shënonim "reserved" që në krijim, pasi do të bllokonte spotin edhe para se të fillojë intervali i rezervuar. Validimi i konfliktit bëhet me Prisma Client të thjeshtë (`findFirst` me `startTime: {lt: end}, endTime: {gt: start}` — mbivendosje standarde intervalesh) — **pa SQL raw**, ndryshe nga zones/spots, sepse `Reservation` s'ka fare kolona PostGIS. Lidhja e vetme me kohën reale: `SessionsService.checkIn` tani refuzon (409) nëse spoti ka një rezervim `confirmed` "aktiv tani" (startTime≤now<endTime) të një përdoruesi tjetër — pikërisht rasti "konflikt rezervimi" i përmendur shprehimisht te §7. Testuar edhe skenari i plotë: user A rezervon një spot 2s në të ardhmen, pritet të bëhet aktiv, user B provon check-in → 409, statusi i spotit mbetet "free" (check-in-i u refuzua para se të prekte DB-në). **Bug mjedisi (jo kod)**: pas shumë cikle preview_start/stop, cache-i dev i Turbopack (`.next/`) u dëmtua dhe `/zones/[id]` ktheu 404 edhe pse skedari ekzistonte korrekt — u zgjidh duke fshirë `.next/` dhe rinisur serverin. Nëse ndodh sërish (route "zhduket" pa ndryshim kodi), provo këtë para se të kërkosh bug në kod. Testuar plotësisht: curl (201/409 mbivendosje/400 e kaluar/400 endTime≤startTime/401/cancel+re-book) + browser i vërtetë (rezervim → shfaqet te "Rezervimet e mia" → anulim live pa refresh).
- **2026-08-29** — Faza 6 (Admin dashboard): `GET /admin/stats` përdor `prisma.parkingSpot.groupBy({by:['status'],_count:true})` — funksionon me Prisma Client të thjeshtë (jo raw SQL) sepse s'zgjedh kolonën `Unsupported` të gjeometrisë, vetëm `status`+numërim. Krijimi i zonave në panel përdor një **drejtkëndësh nga 4 kufij numerikë** (min/max lng/lat) në vend të vizatimit mbi hartë — vizatim real (p.sh. me `@mapbox/mapbox-gl-draw` ose turf.js) do të ishte shtrirje e konsiderueshme jashtë "menaxhim bazë zonash/spotesh"; e lashë si "polish" i mundshëm i mëvonshëm nëse duhet. Faqja `/admin` bën role-gate në klient (`user?.role === 'admin'`) — nuk ka ende middleware/server-side route protection (do të ishte relevante nëse shtohet SSR i ndjeshëm më vonë). **Vërejtje testimi**: browser-i i sandbox-uar e **anulon automatikisht** `window.confirm()` (native dialog) — butonat "Fshij" që e përdorin duken sikur "s'bëjnë asgjë" kur klikohen nga automatizimi, edhe pse kodi është korrekt (verifikuar duke anashkaluar `window.confirm = () => true` përkohësisht dhe duke ripërsëritur klikimin — funksionoi). Kjo s'është bug; mos e "rregullo" duke hequr `confirm()`-in real nga UI. Testuar plotësisht në browser real: stats të sakta, krijim zone→spot, ndryshim statusi (reflektohet në backend), fshirje (verifikuar me confirm të anashkaluar), RBAC (citizen merr "S'ke akses", s'e sheh lidhjen "Paneli admin" në header, `/admin/stats` kthen 403 nga backend).
- **2026-08-29** — Faza 7 (Njoftimet): në vend të polling-ut për njoftimet, u zgjerua **i njëjti** Gateway/Redis i Fazës 3 me room-e private për-përdorues — arkitekturisht më koherente dhe demonstron më mirë "diferencuesin kryesor" (§1). `RealtimeGateway.handleConnection` verifikon JWT-në nga `client.handshake.auth.token` (kërkoi `JwtModule.register({})` edhe në `RealtimeModule`, jo vetëm në `AuthModule`, pasi ai është lokal për modulin). Frontend: `lib/socket.ts` i kalon `auth` si **funksion** (`(cb) => cb({token})`), jo objekt statik — rivlerësohet në çdo (ri)lidhje, kështu që token-i i freskët (pas refresh-it) përdoret gjithmonë. **Ndryshim arkitekturor i domosdoshëm**: `useParkingSocket` më parë e shkëpuste socket-in e përbashkët në `cleanup` (kur dilje nga një faqe zone) — kjo do ta thyente lidhjen e njoftimeve të `useNotificationsSocket` (Header, gjithmonë e montuar), pasi të dyja ndajnë të njëjtin socket singleton nga `getSocket()`. U hoq `socket.disconnect()` nga cleanup-i i `useParkingSocket` (lë vetëm room-at e zonës); shkëputja tani bëhet eksplicitisht vetëm në `Header`'s `handleLogout`. Job-i i kujtesës (`checkReminders`, `@Interval` 60s) kontrollon rezervime me `startTime` brenda 15 min nga tani (jo më herët se tani, ndryshe rezervimi "ka kaluar" dhe s'është më "i ardhshëm") — testuar me lead kohor >60s (jo <60s, përndryshe rezervimi mund të "kalojë" para se cikli tjetër i intervalit ta kapë, siç ndodhi në provën e parë me lead 20s). Testuar tërësisht: curl (REST i plotë + job kujtese, konfirmuar `reminderSent` flag), script Node me socket të autentikuar (mori `notification:new` live), **dhe browser real**: check-in i jashtëm (curl) shfaqi njoftim të ri LIVE në krye të `/notifications` pa refresh dhe rriti numëruesin e ziles në header nga 5→6, "shëno si lexuar"/"shëno të gjitha" ulën numëruesin saktë.
- **2026-08-29** — Faza 8 (Audit logs + rate-limiting): `AuditLog` model i ri (`actorId` opsional + `onDelete: SetNull` — një log s'duhet të zhduket nëse aktori fshihet më vonë) me `AuditLogService.record(action, actorId, metadata?)`/`findRecent()`; `GET /audit-logs` admin-only. Thirret nga `auth.service.ts` (`auth.register`, `auth.login`, `auth.login_failed`) dhe nga `zones`/`spots` service-t te çdo shkrim. **Ndryshim i qëllimshëm mes zones dhe spots**: te `ZonesService`, `actorId` është i detyrueshëm (çdo shkrim vjen nga admin i kyçur via controller); te `SpotsService`, `actorId` është **opsional** sepse `SensorSimulatorService` thërret të njëjtin `spotsService.update()` pa aktor (nuk ka kuptim ta "atribuosh" tek dikush) — audit-logging bëhet vetëm `if (actorId)`. Rate-limiting: `@nestjs/throttler` (stable 6.5.0) kërkon Nest ≤11 si peer-dep, projekti është në Nest 12 → ERESOLVE në instalim; në vend të instalimit të një pakete jo-të-verifikuar/të papërputhshme, u ndërtua `RateLimitGuard` vetjak (`common/guards/`) mbi `RedisService` ekzistues — fixed-window `INCR`+`EXPIRE` te `ratelimit:{method}:{route}:{ip}:{bucket}`, regjistruar global si `APP_GUARD` në `app.module.ts` me limit default 300/60s, dhe `@RateLimit({limit:5, windowSec:60})` të veçantë te `/auth/register`+`/auth/login` kundër brute-force. Frontend: login/register tregojnë mesazh të veçantë për 429. `useZones`/`useSpots` mutation-et invalidojnë tani edhe `["admin","auditLogs"]` që tabela e audit log-ut në panel të freskohet live pas çdo veprimi CRUD admin, jo vetëm pas refresh faqeje. Testuar plotësisht: curl (audit entries korrekte për login/login_failed/zone.create me `actorEmail` të saktë; RBAC 403 për jo-admin; 429 i trigger-uar saktë pas 5 tentativave login brenda dritares) **dhe browser real**: login admin → `/admin` → tabela Audit Log shfaqi historikun me etiketa shqip → krijim "Zona Live Test" nga UI → u shfaq LIVE në tabelë pa refresh (konfirmon wiring-un e invalidation-it kryq) → fshirje (me `window.confirm` të anashkaluar, shih shënimin e Fazës 6) → verifikuar me curl.
- **2026-08-29** — Faza 9 (Analitika e avancuar): **2 endpoint të reja** te i njëjti `AdminController`/`AdminService` (jo modul i veçantë — janë vazhdim natyror i `/admin/stats`): `GET /admin/analytics/heatmap?days=N` (GeoJSON FeatureCollection, `weight` = aktivitet zënieje/spot) dhe `GET /admin/analytics/peak-hours?days=N` (24 kova orësh). **Vendim i qëllimshëm i burimit të të dhënave**: "aktiviteti i zënies" bashkon `SensorEvent` (tranzicione occupied nga simulatori, Faza 3) **+ `ParkingSession.checkIn`** (check-in manual, Faza 4) — jo vetëm `SensorEvent`, edhe pse komenti origjinal i skemës (§3, shtuar në Fazën 3) e lidhte heatmap-in vetëm me të; u zbulua se `SessionsService.checkIn`/`checkOut` ndryshojnë `parking_spots.status` me `$executeRaw` direkt brenda transaksionit (jo via `SpotsService.update()`), kështu që **s'krijojnë kurrë `SensorEvent`** — një heatmap vetëm-SensorEvent do të linte jashtë tërësisht sinjalin e check-in-eve reale, burimin e dytë "të vërtetë" të të dhënave sipas §4. Të dyja query-t përdorin `$queryRaw` (PostGIS + UNION mbi dy tabela) me `make_interval(days => ${days}::int)` — **cast `::int` i domosdoshëm**: Prisma i dërgon parametrat numerikë të `$queryRaw` si `bigint`, dhe `make_interval` s'ka overload për `bigint` (`ERROR 42883: function make_interval(days => bigint) does not exist`), gabim i kapur vetëm në runtime (curl), jo në compile-kohë TypeScript — kontrollo këtë nëse shtohen parametra të tjerë numerikë te raw SQL me funksione Postgres të tipizuara ngushtë. `AnalyticsQueryDto.days` (class-validator, `@Min(1) @Max(90)`) mbrон nga dritare të pavlefshme/të gjata; `RolesGuard` e kontrollon rolin **para** validimit të DTO-s (jo-admin merr 403 edhe me `days` të pavlefshëm, jo 400) — sjellje korrekte e rendit të guard-ave, jo bug. Frontend: `HeatmapMap.tsx` (komponent i ri, jo `ParkingMap` — layer tjetër, `type: "heatmap"`) përdor **layer-in nativ `heatmap` të MapLibre GL** (interpolim `heatmap-density`→ngjyrë, peshë e normalizuar `weight/maxWeight`) — pa librari shtesë (turf.js/deck.gl u shmangën, në linjë me vendimin e Fazës 6 për vizatim zonash). "Orët e pikut" janë bar-chart CSS/div (jo Chart.js/Recharts) — arsyeja e njëjtë e minimalizimit të varësive. Testuar plotësisht: curl (heatmap+peak-hours 200 me `days=30`, gabimi `make_interval` para fix-it, RBAC 403 qytetar, validim 400 me `days=999`) **dhe browser real**: paneli "Analitika" në `/admin` → selektori 7/14/30 ditë rifreksoi të dy query-t me query param të saktë (konfirmuar via network requests) → bar-chart-i i orëve të pikut u rendua me lartësi proporcionale korrekte me të dhënat nga API → harta e heatmap-it u montua (kontrolli `attribution` + canvas MapLibre prezent, pa gabime style/layer në konsolë) — përmbajtja pamore e canvas-it WebGL s'verifikohet vizualisht në këtë browser të sandbox-uar (kufizim i njohur nga Faza 2, shih §8 atje); rekomandohet verifikim vizual nga Gent në browser real.
- **2026-08-29** — Faza 10 (Polish UI/UX): fushëveprimi u caktua eksplicitisht me Gent-in nga 3 opsione (basemap real / vizatim zonash mbi hartë / polish i përgjithshëm) — **u zgjodhën basemap real + polish i përgjithshëm**; **vizatimi i zonave mbi hartë u shty me vetëdije** për shkak të rrezikut të konfliktit peer-dependency mes librarive draw (`@mapbox/mapbox-gl-draw`/`terra-draw`) dhe `maplibre-gl` v6 (aktual në projekt) — pa u provuar ende, por rreziku u vlerësua i ngjashëm me rastin real të `@nestjs/throttler` (Faza 8); formulari me 4 kufij numerikë mbetet siç ishte. **Basemap**: tiles OSM raster publike (`tile.openstreetmap.org`, pa API key) — stil i ri i përbashkët `lib/map-style.ts` (`BASEMAP_STYLE`), përdorur nga `ParkingMap` DHE `HeatmapMap` (zëvendëson `BLANK_STYLE` të secilit, hequr plotësisht). Ngjyrat e layer-it të zonave (`zones-fill`/`zones-outline`) u forcuan pak (opacity 0.12→0.2, outline më i errët `#2563eb`) për kontrast mbi tiles reale në vend të sfondit bosh gri. **Zbulim i rëndësishëm mbi mjedisin e testimit**: kufizimi i dokumentuar që nga Faza 2 ("Browser pane-i i sandbox-uar s'e rendon dot canvas-in WebGL të MapLibre-s") **doli i pjesshëm** — layer-at raster (tiles OSM si imazhe të teksturuara) RENDEROHEN plotësisht dhe u verifikuan vizualisht me screenshot (rrugë, ndërtesa, etiketa Prizreni të dukshme); kufizimi origjinal mund të ketë qenë specifik për BLANK_STYLE bosh (pa burime) ose layer-a vektorë (fill/circle/heatmap) mbi sfond bosh — s'u hulumtua më tej pasi s'ishte më bllokues. **Polish i përgjithshëm**: (1) komponent i ri `components/ui/skeleton.tsx` (pattern standard shadcn, `animate-pulse bg-muted`) zëvendëson tekstin "Duke ngarkuar..." në 8 vende (admin stats/zones-panel/spots-panel/analytics-panel/audit-log-panel, harta publike, zone-detail, rezervimet, njoftimet) — disa prej tyre (zones-panel, spots-panel) s'kishin fare gjendje loading më parë, shtuar për konsistencë; (2) `Header.tsx` u bë responsive: nën breakpoint-in `sm` (640px) lidhjet e nav-it (njoftime/rezervime/admin/email/dil) fshihen pas një butoni hamburger (☰/✕, `useState` lokal, pa librari) — më parë 4-5 elemente në një rresht do të mbivendoseshin/dilnin jashtë ekranit në mobile; verifikuar në browser me viewport mobile (586px) që meny-ja hapet/mbyllet saktë dhe harta mbetet e padëmtuar. `npx tsc --noEmit` dhe `npm run lint` kaluan pa gabime; testuar plotësisht në browser real (jo vetëm DOM/tekst si më parë — këtë herë edhe vizualisht, falë zbulimit mbi raster tiles): basemap OSM i dukshëm në hartën publike DHE në heatmap-in admin, atribucioni OSM i pranishëm, meny-ja mobile funksionale, bar-chart-i i orëve të pikut i padëmtuar.
- **2026-08-29** — Faza 11 (Testim): fushëveprimi u caktua eksplicitisht me Gent-in nga 3 opsione (vetëm backend / +frontend unit / +Playwright e2e i vërtetë) — **u zgjodh vetëm backend**, pasi backend-i tashmë kishte infrastrukturën e gatshme (vitest+supertest+@nestjs/testing nga scaffold-i Nest, shih §2), ndërsa frontend-i s'kishte fare mjete testimi ende. **Unit** (Prisma/varësi të mock-uara, `*.spec.ts` pranë çdo moduli): `sessions.service.spec.ts` (checkIn/checkOut, të 4 degëzimet e konfliktit përfshi race-condition-in e compare-and-swap-it), `reservations.service.spec.ts` (validim intervali, spot i çaktivizuar, mbivendosje, cancel idempotent), `rate-limit.guard.spec.ts` (limit i personalizuar vs. default 300/60s, prag saktë, bypass jo-HTTP). **E2E** (supertest, DB/Redis reale dev — s'u krijua DB i veçantë testimi, thjeshtim i qëllimshëm si te app.e2e-spec.ts default-i i Nest-it): `auth.e2e-spec.ts`, `sessions.e2e-spec.ts`, `reservations.e2e-spec.ts`. Zones/spots/admin/audit-log/notifications **s'kanë e2e teste** — mbuluar tashmë gjerësisht me curl+browser manual në fazat e tyre përkatëse (2,6,7,8,9); u dha përparësi vlerës më të lartë (logjika e konfliktit, race conditions) brenda kohës në dispozicion. **3 probleme reale të gjetura vetëm nga vetë testet (jo nga kodi i aplikacionit)**: (1) `make_interval`-like befasi: Guard-et e Nest-it ekzekutohen PARA Pipe-ve globale (ValidationPipe) — çdo test e2e duhej ta rindërtonte manualisht `ValidationPipe({whitelist,forbidNonWhitelisted,transform})` (`test/utils/create-test-app.ts`), përndryshe DTO-të e pavlefshme s'do të ktheninin kurrë 400; (2) `SensorSimulatorService` (Faza 3) është `@Interval` global, i lidhur me `AppModule` të plotë që testet e2e e importojnë — pa e çaktivizuar, do të kishte ndryshuar rastësisht statuse spotesh gjatë testeve dhe do t'i bënte "flaky" (jo-deterministike); zgjidhur duke vendosur `process.env.SENSOR_SIMULATOR_ENABLED='false'` PARA se `Test.createTestingModule` të kompilonte AppModule-in (dotenv i `ConfigModule.forRoot()` s'i mbishkruan variabla ekzistuese, kështu që kjo mbetet efektive edhe pse `.env` ka `true`); (3) **bug real testesh** (jo aplikacioni): `RateLimitGuard` (Faza 8) përdor çelësa Redis me dritare fikse 60s GLOBALË (jo të lidhur me skedarin e testit) — testi i dedikuar i rate-limit-it te `auth.e2e-spec.ts` që e ezauron QËLLIMISHT limitin e `/auth/login` (5/60s) e linte dritaren "të helmuar" për çdo skedar tjetër që ekzekutohej më pas brenda së njëjtës 60s (i gjithë suite-i zgjat ~25-30s) — `sessions.e2e-spec.ts`/`reservations.e2e-spec.ts` merrnin 429 në vend të 200 kur provonin të kyçeshin si admin. Zgjidhur me `test/utils/reset-rate-limits.ts` (fshin `ratelimit:*` te Redis) të thirrur në HYRJE të `beforeAll`-it të ÇDO skedari e2e (jo vetëm atij që e shkakton), plus rireset pas vetë testit të rate-limit-it — mbrojtje "belt-and-suspenders" pavarësisht rendit të ekzekutimit. Gjithashtu: `vitest.config.e2e.ts` mori `fileParallelism: false` (skedarët e2e ndajnë të njëjtin Postgres/Redis dev, s'duhet të vrapojnë paralel) dhe `testTimeout: 15000` (disa teste presin qëllimisht ~700ms që një rezervim të bëhet "aktiv tani", shih më poshtë). **Testi i konfliktit rezervim↔check-in** (skenari më i vështirë i Fazës 5) përdor `setTimeout(700ms)` real pas krijimit të një rezervimi me `startTime` +500ms në të ardhmen, që rezervimi të "aktivizohet" para se check-in-i i dikujt tjetër të provohet — teknikë e pranueshme vetëm në e2e (jo në unit teste, ku koha do të duhej të mockohej). Fixtures (zona+spote) krijohen duke thirrur direkt `ZonesService`/`SpotsService` (jo SQL i përsëritur, jo HTTP+RBAC) — kërkon `actorId` real (FK e vërtetë te `User`), prandaj testet identifikohen si admin i mbjellë (`prisma/seed.js`) përpara krijimit të fixtures. `afterAll` në çdo skedar e2e ka kontrolle defensive (`if (zoneId)` etj.) — nëse `beforeAll` dështon në gjysmë, `afterAll` s'duhet të hedhë një gabim TË DYTË që fsheh shkakun real të parë. Verifikuar: `npm run test` (21/21 unit), `npm run test:e2e` (31/31 e2e, ekzekutuar disa herë radhazi pa "flakiness"), `npm run lint` (oxlint, pa gabime), dhe një kontroll manual psql pas suite-s që konfirmoi ZERO të dhëna testesh të mbetura (`E2E%` zona, emailat e testeve) në DB-në reale të zhvillimit.
- **2026-08-29** — Faza 12 (Dockerizim final + udhëzues deploy): **Kufizim i pranuar që në fillim**: krijimi real i llogarive Vercel/Railway, lidhja e OAuth-it, dhe klikimi "Deploy" janë veprime që Claude s'i kryen dot vetë (asnjë CLI `vercel`/`railway` s'ishte instaluar/autentikuar në këtë makinë) — fushëveprimi u kufizua qëllimisht te gjithçka e automatizueshme lokalisht (Dockerfile-t e prodhimit, hardening, verifikim real me `docker build`/`docker run`), plus një udhëzues hap-pas-hapi (README.md §Deployment) për pjesën që kërkon domosdoshmërisht ndërveprim njerëzor me dashboard-et. **Dockerfile-t bënë multi-stage me target-e të emërtuara** (`dev` + default-i i prodhimit) në vend të skedarëve të veçantë `Dockerfile`/`Dockerfile.prod` — `docker-compose.yml` lokal specifikon tani `target: dev` (mban `npm run start:dev`/`npm run dev` + volume-mount për hot-reload, siç ishte gjithmonë), ndërsa Railway (backend) e ndërton pa `--target`, duke marrë automatikisht stage-in final `runner`. **Backend runner**: `node_modules` e PLOTË (jo `--omit=dev`) kopjohet nga stage-i `build` — jo optimizimi më i vogël i mundshëm, por i domosdoshëm sepse `prisma` CLI (dev dependency) duhet i pranishëm në runtime për `npx prisma migrate deploy`, i cili rirendet te `CMD` në çdo nisje kontejneri (idempotent + i mbrojtur nga advisory lock i Prisma-s — i sigurt edhe me shumë replika, e pranueshme për shtrirjen e vetme e këtij projekti). **Frontend runner**: kërkoi `output: "standalone"` në `next.config.ts` (`.next/standalone` + `.next/static` kopjohen te imazhi final); `NEXT_PUBLIC_*` gdhenden në bundle NË KOHË BUILD-i (jo runtime), kështu që duhen kaluar si `--build-arg`, jo si env e kontejnerit — dokumentuar te README. **Vercel e injoron plotësisht këtë Dockerfile** (ndërton Next.js nativisht) — skedari shërben vetëm si alternativë self-hosting/testim lokal prodhimi. **CORS hardening**: `main.ts` lexon tani `FRONTEND_URL` (opsionale, e ndarë me presje për shumë origjina) për të kufizuar CORS në prodhim — pa të, sillet si më parë (`origin: true`, i përshtatshëm vetëm për dev lokal). **2 bug real gjetur vetëm nga build-i i pastër Docker (jo nga `npm install`/`npm test` lokal)**: (1) **drift i heshtur i lock file-it**: `npm ci` brenda kontejnerit dështonte me "Missing: magicast@0.3.5 from lock file" edhe pse `npm install`/`npm install --package-lock-only` lokal e raportonin `package-lock.json`-in "up to date" (npm lokal, i instaluar në node_modules ekzistues, s'e rivlerëson gjithmonë plotësisht pemën); shkaku i vërtetë ishte (2) **mospërputhje versioni npm**: lock file-i u gjenerua lokalisht me npm 11.11.0 (i instaluar në këtë makinë), kurse `node:22-alpine` vjen me npm 10.9.8 — `npm ci` i npm 10 e refuzon një lock file të shkruar nga npm 11 si "jo në sinkron" (validim strikt ndër-versionesh, jo problem real i varësive). Zgjidhur me `RUN npm install -g npm@11` si hap i parë në të dy Dockerfile-t (base stage), PARA `npm ci`. **Bug i 3-të, i zbuluar gjatë vetë rregullimit të drift-it të mësipërm**: `rm -rf node_modules && npm install` lokal e rregulloi drift-in, por la `@prisma/client` të pagjeneruar (s'ka `postinstall` script) — çdo tip i importuar prej tij (`Role`, `User`, `SpotStatus`, etj.) u bë papritmas i pagjendshëm, ~50 gabime TS në `tsc --noEmit`, kapur vetëm sepse Faza 12 e rirendi typecheck-un si hap verifikimi (jo pjesë e `npm test`/`npm run build`, që të dyja PRESIN Prisma Client tashmë të gjeneruar nga një hap i mëparshëm dhe s'do ta kishin zbuluar mungesën). Zgjidhur menjëherë (`npx prisma generate`) DHE parandaluar për të ardhmen duke shtuar `"postinstall": "prisma generate"` te `package.json` — çdo `npm install`/`npm ci` i ardhshëm (lokal, CI, apo brenda Docker-it) e gjeneron automatikisht. Kjo nga ana e vet krijoi një konflikt të katërt: `postinstall` nis GJATË `npm ci`, PARA `COPY . .`, kështu që `prisma generate` s'e gjente `schema.prisma` brenda Docker build-it — zgjidhur duke shtuar `COPY prisma ./prisma` menjëherë pas `COPY package.json package-lock.json ./` (para `npm ci`) te stage-i `base`, çka gjithashtu bëri të panevojshëm hapin e veçantë `RUN npx prisma generate` që ekzistonte më parë te stage-t `dev`/`build` (hequr, `postinstall` tashmë e mbulon). **Verifikuar realisht, jo vetëm `docker build`**: të dy imazhet e prodhimit u ndërtuan me sukses, backend-i u nis si kontejner test i lidhur në rrjetin e `docker-compose` (`smart-parking-prizren_default`) kundrejt Postgres/Redis REALE të dev-it — `prisma migrate deploy` gjeti "No pending migrations" (konfirmon lidhjen korrekte), `GET /admin/stats` pa token ktheu 401 (guard-et aktivë), `POST /auth/login` me kredencialet e admin-it ktheu 200 me tokens të vlefshëm (bcrypt+JWT funksionojnë në musl/Alpine, jo vetëm glibc/Windows-dev); frontend-i u nis si kontejner standalone dhe ktheu 200 me përmbajtje korrekte në `/`. Target-i `dev` i backend-it u rindërtua gjithashtu për të konfirmuar që `docker-compose.yml` lokal vazhdon të punojë pa u thyer nga ndryshimet (dhe u ripërsërit edhe pas fix-it të `postinstall`/`COPY prisma`, meqë ndryshimi i parë e thyente). Të dyja imazhet e prodhimit u rindërtuan edhe njëherë pas gjithë fix-eve dhe u ritestuan plotësisht (login+`/admin/stats`), plus `npm test`/`npm run test:e2e`/`npm run lint` lokal u rirendën për të konfirmuar që `rm -rf node_modules && npm install` + `postinstall` i ri s'kishin thyer asgjë (21/21 unit, 31/31 e2e, 0 gabime lint, 0 të dhëna testesh të mbetura në DB). Të gjithë kontejnerët/imazhet e testimit u pastruan (`docker rm`/`docker rmi`) pas verifikimit.
- **2026-08-30** — Faza 13 (Dokumentim, faza e fundit e roadmap-it): fushëveprimi u caktua me Gent-in nga 3 opsione (Swagger/OpenAPI / diagram Mermaid / asnjë) — **u zgjodhën të dyja**. **Swagger**: `@nestjs/swagger` (v12, kompatibël me Nest 12, instaluar pa konflikt — ndryshe nga `@nestjs/throttler` i Fazës 8) + plugin-i CLI i tij te `nest-cli.json` (`compilerOptions.plugins`) — gjeneron `@ApiProperty()` automatikisht nga tipet TS + decorators class-validator të DTO-ve ekzistuese NË KOHË BUILD-it, pa dashur të shtohen decorators shtesë manualisht në çdo DTO (verifikuar: `RegisterDto` shfaqi saktë `email`/`password` me shembuj të gjeneruar nga `@IsEmail()`/`@MinLength(8)`). UI-ja montohet te `/docs` (jo `/api`, për të mos u ngatërruar me rrugë të mundshme të ardhshme `/api/*`), **gjithmonë aktive** (jo vetëm dev) — vendim i qëllimshëm: projekt portofoli, dokumentimi i API-t është pjesë e dorëzimit, jo sekret prodhimi që duhet fshehur pas `NODE_ENV`. `@ApiTags()`/`@ApiBearerAuth()` u shtuan manualisht te të 8 controllers (class-level kur GJITHË rrugët e një controller-i janë të mbrojtura — sessions/notifications/admin/audit-logs; method-level kur janë të përziera — auth/zones/spots/reservations) — grupimi + ikona e kyçit u verifikuan vizualisht në browser. **Diagram arkitekture**: flowchart Mermaid i ri te README.md §Architecture (GitHub e rendon nativisht, pa libra shtesë) — vizualizon burimet e të dhënave (sensor/check-in/rezervim) → Postgres + Redis pub/sub → Gateway → 2 lloje room-esh (zonë publike vs. user privat) → patch i cache-it të React Query → flash i hartës; theksohet shprehimisht se njoftimet ripërdorin TË NJËJTIN pipeline si statuset e spoteve (jo arkitekturë e dyfishtë) — pikërisht "diferenciuesi kryesor" i §1. Testuar plotësisht: `tsc --noEmit`/`oxlint` pa gabime, `npm test` (21/21) + `npm run test:e2e` (31/31) pa regresion, Swagger UI e inspektuar në browser real (endpoint-e/tags/schemas/lock-icons korrekte), **dhe rindërtim i plotë i imazhit Docker të prodhimit** për të konfirmuar që plugin-i CLI i Swagger-it (hap shtesë kompilimi) s'e thyen `nest build` brenda kontejnerit. **Me këtë faz, roadmap-i origjinal i §6 (Faza 0-13) është plotësisht i mbyllur.**

## 9. Iniciativa "SaaS/Smart-City redesign" (2026-08-30 →, jashtë roadmap-it origjinal §6)

Gent solli një prompt të gjatë master (branding, design system, citizen/admin UX, i18n, 2FA/OTP, Vehicles, Device management, PWA, etj. — shih historikun e bisedës për tekstin e plotë) me kërkesë eksplicite për ekzekutim të vazhdueshëm shumë-fazor pa u ndalur për aprovim mes fazave (override i qëllimshëm i rregullit §7 "kërko aprovim", vetëm për këtë iniciativë). **Audit fillestar (Faza 1 e promptit)** zbuloi se ~1/3 e kërkesave kërkojnë arkitekturë krejt të re (jo redesign UI mbi diçka ekzistuese): s'ka model `Vehicle`, s'ka infrastrukturë i18n/lokalizimi, s'ka 2FA/OTP/email-verification/forgot-password në backend (vetëm register/login/refresh/logout/Google), s'ka dërgim email-i fare, s'ka entitet `Device`/`Sensor` i dallueshëm nga `SensorEvent` (pa heartbeat/battery), s'ka çmimtari/revenue. **Vendime fushëveprimi të konfirmuara me Gent** (AskUserQuestion, jo supozim): (1) Vehicles, Device/Sensor entity, dhe i18n SQ/EN/SR — **të shtyra**, jashtë këtij run-i (kërkojnë modele+migrime+CRUD të plota, shtrirje shumë e madhe); (2) forgot-password/email-verification/OTP — **ndërtohen realisht**, me **Resend** si provider email-i (Gent do të vendosë `RESEND_API_KEY` te `.env` lokal kur të jetë gati; integrimi lexon variablën, s'bllokon zhvillimin para se çelësi të vihet).

**Gjetje e rëndësishme e auditit**: shumë prej §2 (Design System) dhe §3 (Auth UX) të promptit ishin **tashmë ndërtuar** nga një sesion i mëparshëm (commit `2341bd9 "Faza 2: Design system - dialog/sheet, status colors, map filters, navbar dhe auth polish"`, pas Faza 14.2-14.5 të padokumentuara më sipër në këtë skedar) — status-color tokens (`--status-*-bg/-fg`) përputhen saktësisht me paletën semantike të kërkuar, dark mode, `AuthLayout`/`GoogleButton`/`PasswordInput`/`IconInput`, `Header` responsive me mobile sheet nav, `StatsCards`/`MapFilters`. **Mos e rindërto këtë** — verifikohet ekzistenca përpara se të shtohet diçka e re (shih §85 e promptit, e ndjekur këtu).

**Slice i parë (kjo sesion, i verifikuar plotësisht në browser)**: restrukturim i `/admin` nga një faqe e vetme e stivosur (`page.tsx` me 4 panele njëra pas tjetrës) në një **shell me sidebar** sipas §8/§22 të promptit — `app/(dashboard)/admin/layout.tsx` (rol-gate i zhvendosur këtu nga `page.tsx`, mbulon të gjitha nën-rrugët; sheet mobile me `AdminSidebarNav`), `components/dashboard/AdminSidebar.tsx` (nav e grupuar OVERVIEW/OPERACIONE/ANALITIKË/ADMINISTRIM, collapse/expand i ruajtur në `useUiStore` me `persist`), nën-rrugë `/admin/{zones,spots,analytics,audit-log}` që thjesht rirendisin panelet EKZISTUESE (`ZonesPanel` etj., të pandryshuara përveç `h2`→`h1`, pasi tani secili panel është përmbajtja e vetme e faqes së vet — jo dyfishim titulli). Shtuar `hooks/useConnectionStatus.ts` + `components/realtime/LiveIndicator.tsx` (§11/§74 "asnjëherë mos e simulo live" — gjendja bazohet vetëm në eventet reale `connect`/`disconnect`/`reconnect_attempt` të socket.io-client mbi singleton-in ekzistues `lib/socket.ts`, jo animacion fals), i integruar në `Header` (desktop+mobile sheet) dhe në top-bar-in e admin-it. Shtuar KPI "Shkalla e zënies" (occupancy rate, i llogaritur klient-side nga `spotsByStatus`/`totalSpots` — jo hardcoded). Verifikuar: `tsc --noEmit`+`npm run lint` pa gabime, dhe browser real (desktop: sidebar collapse/expand+navigim mes 4 nën-faqeve+active-state; mobile 375px: sheet i majtë hapet/mbyllet me nav të grupuar; RBAC gate ende funksionon). **Vërejtje jo-bllokuese e gjetur gjatë testimit** (para-ekzistuese, jo e shkaktuar nga ky ndryshim — thjesht e zhvendosur nga `page.tsx` te `layout.tsx`): guard-i `!user`/`role!=='admin'` mund të "flash"-ojë shkurtimisht "Duhet të kyçesh" në navigim të plotë (jo client-side) para se `useAuthStore`'s zustand-persist të rihidratohet nga localStorage — s'u rregullua këtë herë (jashtë fushëveprimit të slice-it), shënuar për "polish" të mëvonshëm nëse shqetëson.

**Slice i dytë (kjo sesion, forgot-password + email-verification me Resend)**: `User` model mori 5 fusha të reja (`emailVerified`, `emailVerificationTokenHash`/`ExpiresAt`, `passwordResetTokenHash`/`ExpiresAt`, migrim `20260830230336_add_password_reset_email_verification`) + 2 indekse mbi token-hash-et. Token-at (email-verifikim 24h, password-reset 1h) janë random 32-byte (`crypto.randomBytes`), ruhen VETËM të hash-uar (`sha256`, jo bcrypt — token-i vetë ka entropi të lartë e rastësishme, jo password i zgjedhur nga useri, kështu që sha256 mjafton dhe është shumë më i shpejtë se bcrypt për verifikim); `/auth/forgot-password` kthen GJITHMONË të njëjtin mesazh (ekziston email-i apo jo) — mbrojtje kundër user enumeration. Rivendosja e fjalëkalimit pastron edhe `hashedRefreshToken` (çkyç çdo sesion tjetër aktiv). **`EmailModule`** (`modules/email/`) mbështjell `Resend` SDK — nëse `RESEND_API_KEY` mungon, regjistron paralajmërim dhe s'dërgon asgjë (s'hedh gabim, s'e bllokon regjistrimin/kërkesën) — Gent e vendosi çelësin gjatë këtij sesioni dhe integrimi u verifikua i lidhur REALISHT me Resend (jo mock): email test drejt `@example.com` u refuzua nga vetë Resend (422, "Invalid to field" — sandbox-i i Resend lejon vetëm adresën e vet llogarisë pa domain të verifikuar) dhe u regjistrua si gabim pa thyer regjistrimin, ndërsa kërkesë reale drejt email-it të Gent-it (`gentkrass21@gmail.com`, llogaria ekzistuese Google) u dërgua pa gabim. Frontend: 3 faqe të reja (`/forgot-password`, `/reset-password`, `/verify-email`, të gjitha `Suspense`-wrapped për `useSearchParams` si `/login`), link "Harrove fjalëkalimin?" te `/login`, dhe artikull "Ridërgo verifikimin e email-it" në menynë e `Header` kur `user.emailVerified===false`. **2 bug real të pa-lidhur me email-in, të gjetur vetëm gjatë verifikimit**: (1) `prisma migrate dev` **rifshiu indekset GiST hapësinore** (`parking_spots_location_gist_idx`, `parking_zones_polygon_gist_idx`) — SAKTËSISHT kurthi i dokumentuar te §8 Faza 3 (Prisma-diff-i s'i njeh si "të qëllimshme" mbi kolona `Unsupported`), por këtë herë u aplikua para se të kontrollohej `migration.sql` (rregull i mësuar: gjithmonë kontrollo skedarin e migrimit të krijuar PARA se `prisma migrate dev` ta aplikojë, ose të paktën menjëherë pas, te ky projekt specifik) — rregulluar duke i rikrijuar të dyja indekset direkt në DB dhe duke pastruar `DROP INDEX`-et nga skedari i migrimit; (2) refresh-token rotation mund të prodhonte **2 JWT identikë bit-për-bit** nëse dy thirrje `issueTokens()` binin brenda së njëjtës sekonde (`iat` ka rezolucion vetëm sekondë sipas spec-it JWT, payload tjetër identik) — zbuluar nga një flake në `auth.e2e-spec.ts` (`refresh rotates the token pair`) që u bë I RIPRODHUESHËM nën ngarkesë CPU (disa dev-server në sfond gjatë testimit) — rregulluar duke shtuar `jti` (random 16-byte) në payload te `issueTokens()`, e cila e bën çdo token unik pavarësisht `iat`; verifikuar me 6+ ekzekutime radhazi të `test:e2e` pa dështim pas fix-it (para fix-it, ~1/3 e ekzekutimeve dështonin nën ngarkesë). Backfill: user-at ekzistues Google (`googleId` jo-null) u vendosën `emailVerified=true` (Google e ka verifikuar tashmë), si pjesë e vetë migrimit + `UPDATE` manual mbi DB-në ekzistuese të dev-it. `test/utils/create-test-app.ts` mori `process.env.RESEND_API_KEY=''` (jo `delete` — dotenv s'i mbishkruan variabla EKZISTUESE, `delete` do ta linte të rimbushej nga `.env` real) para se AppModule të kompilohej, njësoj si mbyllja e `SENSOR_SIMULATOR_ENABLED` (Faza 11) — pa këtë, çdo `npm run test:e2e` do të thërriste Resend-in real me email-e `@example.com` (422 të pafajshëm, por harxhim kuote+zhurmë logu). Testuar plotësisht: `tsc --noEmit`+`npm run lint` pa gabime (frontend+backend), `npm run test` (21/21), `npm run test:e2e` (31/31, disa herë radhazi), **dhe browser+curl real**: forgot-password→email real i dërguar→reset-password me token real (validim mospërputhjeje fjalëkalimesh, ndryshim i suksesshëm, fjalëkalimi i vjetër refuzohet pas, token i përdorur s'ripërdoret/400)→verify-email (sukses + rast token i pavlefshëm)→banner "ridërgo verifikimin" në Header (200 real, konfirmuar via network requests). Të dhënat e testit (`@example.com`) u pastruan nga DB pas verifikimit.

**Mbetet (backlog i sinqertë, jo i pretenduar si i bërë)**: 2FA/TOTP (jashtë fushës së kësaj here — "OTP" u interpretua si email-verifikim me lidhje, jo app-based TOTP me QR/backup-codes, shtrirje dukshëm më e madhe), faqe/tabelë Users Management (§29), System Health (§32), Alert Center (§59), UI e plotë Attendant (§30, aktualisht skedar pothuajse bosh), ridizajnim i hartës operacionale admin (§23), drawer/flow të detajuara parkimi/rezervimi/sesionit (§12-14), audit i plotë WCAG (§37), PWA (§81), faqe profili/settings (§18-19, s'ekziston ende — banner-i i verifikimit u vendos përkohësisht te Header dropdown në vend të një faqeje settings). Vazhdohet në sesionet vijuese sipas §0 të promptit (pa u ndalur për aprovim), me përditësim të këtij logu pas çdo slice-i të ri.
